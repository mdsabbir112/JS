// Sort the Array using only the sort() function . it not working well to contrast the overall sorting in the array.

let numbers = [6,29,9,5,4,1,3];
// .sort((a, b) => a - b)
// numbers.sort();
numbers.sort((a, b) => a - b); // This is the way to solve the problem on behalf of sorting something properly.
console.log(numbers);
